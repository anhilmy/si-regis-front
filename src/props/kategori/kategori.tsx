export type SummaryKategori = {
    total_kegiatan: number,
    id: number,
    nama: string,
    is_active: string
}

export type Kategori = {
    id: number,
    nama: string,
    is_active: boolean

}